import { usePortalStore } from "store/portal";

export function getValueInfo(GKey: any) {
  return null;

  // const clientInfo = usePortalStore((state) => state.clientInfo);
  // if (clientInfo.filter((item) => item.groupKey == GKey).length > 0) {
  //   return clientInfo.find((item) => item.groupKey == GKey).properties[0]
  //     .propertyValue;
  // } else {
  //   return null;
  // }
}
